// AiR runtime configuration for the published static site.
//
// Set apiOrigin to the HTTPS origin of your AiR server (the Node/tRPC backend)
// to turn on sign-in, saved progress, community, uploads, and admin tools.
// Leave it empty to run the public learning site on its own.
//
// Example: window.AIR_CONFIG = { apiOrigin: "https://api.your-domain.org" };
window.AIR_CONFIG = { apiOrigin: "" };
